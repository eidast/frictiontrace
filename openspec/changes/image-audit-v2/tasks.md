# Tasks

- [ ] 1.1 Schema: additive columns on `image_findings` (`form_factor` NOT NULL DEFAULT 'mobile', `displayed_width` REAL, `displayed_height` REAL) and `synthetic_runs` (`image_bytes_modern`, `image_bytes_legacy`, `image_bytes_third_party` REAL, `image_count` INTEGER) in `engine/src/crux/schema.ts`; PRAGMA/ALTER migrations in `engine/src/crux/db.ts` for existing DBs.
- [x] 1.2 Runner: `--form-factor mobile|desktop|both` flag (default mobile); desktop uses Lighthouse desktop emulation and a new `broadband` throttling profile (RTT 40 ms, 20/5 Mbps, 1× CPU). Targets expand per form factor; `synthetic_runs.form_factor` and `throttling_profile` reflect reality.
- [x] 1.3 Runner: persist `form_factor` and `displayed_width/height` (from `image-delivery-insight` item `node.boundingRect`) on `image_findings`, in both sequential and parallel-worker paths (extend worker result message).
- [x] 1.4 Runner: LCP checks — from `largest-contentful-paint-element` + `lcp-discovery-insight` emit findings `lcp-lazy-loaded` / `lcp-missing-fetchpriority` / `lcp-not-discoverable` (NULL bytes) for the LCP image URL.
- [x] 1.5 Runner: `vtex-fullres-image` — scan `network-requests` image items; VTEX CDN hosts without `-{w}-{h}` path segment → finding with total_bytes = transferSize.
- [x] 1.6 Runner: per-page image stats (modern/legacy/third-party bytes, image count) from `network-requests` → new `synthetic_runs` columns.
- [ ] 1.7 Optimizer: `npm install sharp`; new `scripts/image-optimize.ts` + npm script `image:optimize` — downloads latest non-excluded homepage finding resources (per origin + form_factor), resizes to displayed ×2 DPR (cap 2560 px), WebP q80, writes `reports/image-assets/<finding_id>.webp` + `manifest.json` (optimized_bytes, fetched_bytes, width, height). Idempotent, concurrency 4, robust to download failures (skip with log).
- [ ] 1.8 Report: clickable finding rows open a modal viewer — original (hotlinked, `referrerpolicy="no-referrer"`, onerror fallback), optimized variant from manifest, byte comparison (fetched original vs optimized vs Lighthouse estimate), "Ver original" link and "Descargar optimizada" (`<a download>`).
- [ ] 1.9 Report: Mobile/Desktop toggle (default mobile) filtering all tables by `form_factor`; new AUDIT_LABELS for the 4 new audit ids; per-site stacked bar of image bytes (modern / legacy / third-party) from the new `synthetic_runs` columns; footer note about the `image-assets/` folder.
- [ ] 1.10 Data: full-cohort run `--page homepage --form-factor both --label image-audit-v2-2026-08 --concurrency 4`, then `npm run image:optimize` and `npm run image:report`; open the HTML and verify modal, download, toggle, and stats visually.
- [ ] 1.11 Verify: `npm run test:unit`, `npm run typecheck`, `openspec validate image-audit-v2 --strict`. Unit tests only for new pure logic if extracted to a testable module (VTEX URL detection, format classification); scripts otherwise keep the repo's existing untested-script pattern.
